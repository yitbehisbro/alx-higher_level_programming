#!/usr/bin/python3
def complex_delete(a_dictionary, value):
	lists = list(a_dictionary.keys())
    for i in lists:
        if j == a_dictionary.get(i):
            del a_dictionary[i]
    return a_dictionary
